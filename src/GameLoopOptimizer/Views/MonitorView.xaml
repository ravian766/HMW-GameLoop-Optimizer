<UserControl x:Class="GameLoopOptimizer.Views.MonitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:GameLoopOptimizer.Views.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="900">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="24,20,24,24">

            <!-- Title Header -->
            <Border Style="{StaticResource GlassCard}" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Hardware Performance Telemetry" FontSize="18" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,4" />
                        <TextBlock Text="Real-time 1-second system resource monitoring and emulator frame-time pacing analytics." FontSize="13" Foreground="#94A3B8" />
                    </StackPanel>

                    <!-- Frame-Time Variance Status -->
                    <Border Grid.Column="1" Background="#182333" CornerRadius="8" Padding="12,6" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="EST. JITTER: " FontSize="11" FontWeight="SemiBold" Foreground="#94A3B8" VerticalAlignment="Center" />
                            <TextBlock Text="{Binding Metrics.EstimatedFrametimeVarianceMs, StringFormat='{}{0:F1} ms'}" FontSize="13" FontWeight="Bold" Foreground="#00E5FF" VerticalAlignment="Center" />
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <!-- 2x2 Real-Time Graphs Grid -->
            <Grid Margin="0,0,0,16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- 1. CPU History Chart -->
                <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource GlassCard}" Margin="0,0,8,16">
                    <StackPanel>
                        <Grid Margin="0,0,0,8">
                            <TextBlock Text="CPU UTILIZATION (%)" FontSize="12" FontWeight="SemiBold" Foreground="#94A3B8" />
                            <TextBlock Text="{Binding Metrics.CpuTotalPercent, StringFormat='{}{0:F1}%'}" FontSize="14" FontWeight="Bold" Foreground="#00E5FF" HorizontalAlignment="Right" />
                        </Grid>
                        <controls:LiveChartControl PointsSource="{Binding CpuHistory}" MaxValue="100" Height="140" LineBrush="#00E5FF" />
                    </StackPanel>
                </Border>

                <!-- 2. GPU History Chart -->
                <Border Grid.Row="0" Grid.Column="1" Style="{StaticResource GlassCard}" Margin="8,0,0,16">
                    <StackPanel>
                        <Grid Margin="0,0,0,8">
                            <TextBlock Text="GPU 3D RASTERIZER (%)" FontSize="12" FontWeight="SemiBold" Foreground="#94A3B8" />
                            <TextBlock Text="{Binding Metrics.GpuPercent, StringFormat='{}{0:F1}%'}" FontSize="14" FontWeight="Bold" Foreground="#7C4DFF" HorizontalAlignment="Right" />
                        </Grid>
                        <controls:LiveChartControl PointsSource="{Binding GpuHistory}" MaxValue="100" Height="140" LineBrush="#7C4DFF" />
                    </StackPanel>
                </Border>

                <!-- 3. RAM History Chart -->
                <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource GlassCard}" Margin="0,0,8,0">
                    <StackPanel>
                        <Grid Margin="0,0,0,8">
                            <TextBlock Text="RAM USAGE (%)" FontSize="12" FontWeight="SemiBold" Foreground="#94A3B8" />
                            <TextBlock Text="{Binding Metrics.RamPercent, StringFormat='{}{0:F0}%'}" FontSize="14" FontWeight="Bold" Foreground="#00E676" HorizontalAlignment="Right" />
                        </Grid>
                        <controls:LiveChartControl PointsSource="{Binding RamHistory}" MaxValue="100" Height="140" LineBrush="#00E676" />
                    </StackPanel>
                </Border>

                <!-- 4. Disk I/O History Chart -->
                <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource GlassCard}" Margin="8,0,0,0">
                    <StackPanel>
                        <Grid Margin="0,0,0,8">
                            <TextBlock Text="DISK THROUGHPUT (MB/s)" FontSize="12" FontWeight="SemiBold" Foreground="#94A3B8" />
                            <TextBlock Text="{Binding Metrics.DiskReadMbSec, StringFormat='{}{0:F1} MB/s'}" FontSize="14" FontWeight="Bold" Foreground="#FFB300" HorizontalAlignment="Right" />
                        </Grid>
                        <controls:LiveChartControl PointsSource="{Binding DiskHistory}" MaxValue="60" Height="140" LineBrush="#FFB300" />
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Detailed Telemetry Card -->
            <Border Style="{StaticResource GlassCard}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="MEMORY HEADROOM" FontSize="11" FontWeight="SemiBold" Foreground="#94A3B8" />
                        <TextBlock Text="{Binding Metrics.RamUsedGb, StringFormat='Used: {0:F1} GB'}" FontSize="13" Foreground="#FFFFFF" Margin="0,4,0,2" />
                        <TextBlock Text="{Binding Metrics.RamAvailableGb, StringFormat='Available: {0:F1} GB'}" FontSize="12" Foreground="#00E676" />
                    </StackPanel>

                    <StackPanel Grid.Column="1">
                        <TextBlock Text="GAMELOOP EMULATOR USAGE" FontSize="11" FontWeight="SemiBold" Foreground="#94A3B8" />
                        <TextBlock Text="{Binding Metrics.GameLoopRamMb, StringFormat='Memory: {0:F0} MB'}" FontSize="13" Foreground="#FFFFFF" Margin="0,4,0,2" />
                        <TextBlock Text="{Binding Metrics.IsGameLoopActive, Converter={StaticResource BooleanToStatusTextConverter}, FallbackValue='Running'}" FontSize="12" Foreground="#00E5FF" />
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="DISK I/O RATE" FontSize="11" FontWeight="SemiBold" Foreground="#94A3B8" />
                        <TextBlock Text="{Binding Metrics.DiskReadMbSec, StringFormat='Read: {0:F1} MB/s'}" FontSize="13" Foreground="#FFFFFF" Margin="0,4,0,2" />
                        <TextBlock Text="{Binding Metrics.DiskWriteMbSec, StringFormat='Write: {0:F1} MB/s'}" FontSize="12" Foreground="#94A3B8" />
                    </StackPanel>
                </Grid>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
