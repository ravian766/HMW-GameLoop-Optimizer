<Application x:Class="GameLoopOptimizer.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:GameLoopOptimizer"
             xmlns:vm="clr-namespace:GameLoopOptimizer.ViewModels"
             xmlns:views="clr-namespace:GameLoopOptimizer.Views"
             xmlns:models="clr-namespace:GameLoopOptimizer.Models"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles/Colors.xaml" />
                <ResourceDictionary Source="Styles/ModernControls.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Global Razor-Sharp Font & Text Rendering Styles -->
            <Style TargetType="{x:Type TextBlock}">
                <Setter Property="FontFamily" Value="Segoe UI, Segoe UI Variable, Arial" />
                <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
                <Setter Property="TextOptions.TextRenderingMode" Value="ClearType" />
                <Setter Property="TextOptions.TextHintingMode" Value="Fixed" />
                <Setter Property="SnapsToDevicePixels" Value="True" />
                <Setter Property="UseLayoutRounding" Value="True" />
            </Style>

            <Style TargetType="{x:Type Control}">
                <Setter Property="FontFamily" Value="Segoe UI, Segoe UI Variable, Arial" />
                <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
                <Setter Property="TextOptions.TextRenderingMode" Value="ClearType" />
                <Setter Property="SnapsToDevicePixels" Value="True" />
                <Setter Property="UseLayoutRounding" Value="True" />
            </Style>

            <!-- Converters -->
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
            <local:BooleanToRunningStatusConverter x:Key="BooleanToRunningStatusConverter" />
            <local:BooleanToStatusTextConverter x:Key="BooleanToStatusTextConverter" />
            <local:EnumToBooleanConverter x:Key="EnumToBooleanConverter" />
            <local:StringToBooleanConverter x:Key="StringToBooleanConverter" />

            <!-- ViewModel DataTemplates -->
            <DataTemplate DataType="{x:Type vm:DashboardViewModel}">
                <views:DashboardView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type vm:OptimizerViewModel}">
                <views:OptimizerView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type vm:GameLoopViewModel}">
                <views:GameLoopView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type vm:MonitorViewModel}">
                <views:MonitorView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type vm:GamingSessionViewModel}">
                <views:GamingSessionView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type vm:BackupViewModel}">
                <views:BackupView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type vm:LogsViewModel}">
                <views:LogsView />
            </DataTemplate>

        </ResourceDictionary>
    </Application.Resources>
</Application>
